{
  "name": "@veritaschain/contracts",
  "version": "0.1.0",
  "description": "VeritasChain smart contracts",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.cjs --network ganache",
    "deploy:cli": "hardhat run scripts/deploy.cjs --network ganacheCLI",
    "node": "hardhat node",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "ethers": "^6.16.0",
    "hardhat": "^2.22.3"
  }
}
